import 'package:flutter/material.dart';

class Printerpage extends StatelessWidget {
  const Printerpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("printer page"),
      ),
    );
  }
}
