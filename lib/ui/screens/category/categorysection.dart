import 'package:flutter/material.dart';

class Categorysection extends StatelessWidget {
  const Categorysection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Category section"),
      ),
    );
  }
}
